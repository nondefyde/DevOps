version: '3.4'
services:
  app:
    image: ${IMAGE}
    command: ${COMMAND}
    restart: always
    volumes:
      - ./:/app
    environment:
      NODE_ENV: ${NODE_ENV}
      VIRTUAL_HOST: ${VIRTUAL_HOST}
    env_file:
      - ./env
    ports:
      - "8000-8020:8000"
